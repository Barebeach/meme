[phases.setup]
nixPkgs = ["nodejs_22", "python3", "python3Packages.pip", "pkg-config", "libudev-zero"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

