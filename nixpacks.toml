[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --force --legacy-peer-deps --no-optional --ignore-scripts || npm install --force --legacy-peer-deps --no-optional"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

